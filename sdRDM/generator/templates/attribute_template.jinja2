{{name}}: {{dtype}} = Field(
    {% if required is true and 'default' not in metadata.keys() %}...,{% endif %}
    {% for key, value in metadata.items() %}
    {{key}}={{value}},
    {% endfor %}
    )

